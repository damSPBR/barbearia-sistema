export default {
  plugins: {
    // Agora usamos o novo pacote que acabamos de instalar
    "@tailwindcss/postcss": {}, 
    // Mantemos o autoprefixer para compatibilidade entre navegadores
    autoprefixer: {},
  },
}